{
  "homepage": "https://github.hubspot.com/calling-extensions-sdk",
  "scripts": {
    "ci:js": "cd demo-minimal-js && npm ci",
    "ci:react": "cd demo-react-ts && npm ci",
    "build:js": "cd demo-minimal-js && npm run build",
    "build:react": "cd demo-react-ts && npm run build",
    "test:react": "cd demo-react-ts && npm run test",
    "ci": "npm run ci:js && npm run ci:react",
    "build": "npm run build:js && npm run build:react",
    "test": "npm run test:react",
    "predeploy": "npm run ci && npm run build && cp ./src/index.html build/index.html && cp -a demo-minimal-js/bin/. build && cp -a demo-react-ts/dist/. build"
  }
}
